# Instalar opencode en Linux

## Vía brew (recomendado)
```sh
brew install anomalyco/tap/opencode
```

## Vía npm (alternativa)
```sh
npm install -g opencode
```

## Verificar
```sh
opencode --version
```
